{% set currentUrl = currentSite.getBaseUrl() %}
<h1 class="w-40">
	<a href="{{ currentSite }}">
		<img src="{{ logo.src(576) }}" data-srcset="{{ logo.srcset() }}" alt="{{ logoAltText }}">
	</a>
</h1>
